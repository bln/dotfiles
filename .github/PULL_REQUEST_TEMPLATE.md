## Summary

Describe the change and its motivation.

## Changed paths

- 

## Validation

- [ ] `mise run test` passes
- [ ] `mise run verify` passes on macOS (when machine state changed)
- [ ] Setup tasks are idempotent (re-run is safe)
- [ ] Destructive operations support dry-run
- [ ] No secrets or machine-specific identity committed
- [ ] Documentation updated
- [ ] No direct package-manager commands added (`brew install`, etc.)
